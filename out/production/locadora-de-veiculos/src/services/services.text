🧱 Pasta: services/ — Regras de Negócio e Lógica do Sistema
locadora_project/
├── src/
│   ├── services/                → Camada de regras de negócio
│   │                              Responsável por processar as informações antes de enviar ou receber dados do banco.
│   │                              Atua como intermediária entre as classes de interface (`ui/`) e os objetos de acesso a dados (`dao/`).
│   │
│   │                              Essa camada concentra as **regras de negócio**, **validações** e **operações complexas**
│   │                              que envolvem mais de uma entidade (como locações, reservas e devoluções).
│   │
│   │   ├── ClienteService.java   → Regras de validação e gerenciamento de clientes
│   │   ├── VeiculoService.java   → Regras de negócio sobre veículos (disponibilidade, categorias, etc.)
│   │   ├── ReservaService.java   → Controle de reservas, disponibilidade e cancelamentos
│   │   ├── LocacaoService.java   → Cálculo de valor total, devoluções e geração de recibos
│   │   └── FuncionarioService.java → (Opcional) Cadastro e autenticação de funcionários

⚙️ Função geral da pasta services/

A camada Service é responsável por:
✅ Validar os dados antes de persistir no banco
✅ Aplicar as regras de negócio
✅ Coordenar operações entre múltiplos DAOs
✅ Evitar que a UI ou o DAO fiquem sobrecarregados com lógica extra
✅ Garantir a consistência das transações (ex: locar um veículo só se estiver disponível)

🔄 Papel na arquitetura
Fluxo geral do sistema:

UI (menus, terminal)
   ↓
Services (validações, cálculos, regras)
   ↓
DAO (CRUD, SQL, banco de dados)
   ↓
MySQL (XAMPP)


📌 O que acontece aqui:
O Service valida o CPF antes de mandar para o DAO.
Impede cadastros duplicados.
Faz mensagens de retorno mais amigáveis para a ui.
Controla o fluxo de CRUD de forma segura.